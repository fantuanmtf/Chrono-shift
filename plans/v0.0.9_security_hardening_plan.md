# Chrono-shift v0.0.9 安全加固版开发计划

> 依据:2026-08-16 安全审计(4 路并行代码审查 + cargo-audit 280 crate 扫描)
> 基线:142/142 测试通过 · clippy 0 错误 · 无硬编码密钥 · 无密钥入库
> 版本:产品 v0.0.9 · Cargo 0.9.0(本版本只做安全加固,不引入新功能)

## P0 · 发布阻塞

| # | 事项 | 位置 | 验收(攻击性测试) |
|---|------|------|------|
| 1 | X25519 低阶点校验 | session.rs:197,278 | 全零 eph → 握手被拒 |
| 2 | 认证结果强制执行 | connection_manager.rs:547,271,348 | 未认证会话仅放行 Ping/Pong;未认证不钉公钥 |
| 3 | Web 控制台 Token 认证 + 防 rebinding | web.rs / index.html | 无 Token 401;伪造 Host/Origin 403 |
| 4 | 中继抗女巫(按公钥记账)+ hops 入站强制 | relay.rs / app.rs | 同密钥多 uid 共享配额;hops>8 丢弃 |
| 5 | 资源上限:连接数 256、入站通道有界、写队列收缩 | connection_manager.rs / app.rs / main.rs | 超限拒绝;队列满丢消息有日志 |

## P1 · 加固

| # | 事项 | 位置 |
|---|------|------|
| 6 | XSS 修复(textContent)+ uid 全局校验 | index.html:118-123 / lib.rs(validate_uid) |
| 7 | protocol_filter 字节切片 panic 修复 + 状态澄清 | protocol_filter.rs:111 |
| 8 | 敏感文件权限(已存在 0644 修复/传输凭据 0600/目录 0700) | identity.rs / transport.rs / storage.rs |
| 9 | 存储回退路径 0600+fsync、WAL 大小上限;FFI rust_free_string + 输入上限 | storage.rs / ffi.rs / parser.rs |
| 10 | LAN 宣告签名 | **推迟**:模块未接线,接线前修复(v0.1.0) |

## P2 · 工程与依赖

- cargo update(清除 quinn-proto RUSTSEC-2026-0185 / anyhow RUSTSEC-2026-0190)
- CI 增加 cargo-audit -D warnings 门禁
- relay 时钟回拨修复、ratchet 死代码标注、移除未用依赖 ct-codecs

## 明确推迟(带理由)

- GCM AAD 上下文绑定:改线上协议,需全节点同步升级 → v0.1.0
- WAL/快照 HMAC:需密钥管理与迁移方案 → v0.1.0
- /api/connect 目标地址白名单:公开 IP 好友是合法场景;由 Token + Host/Origin 校验替代
- 连接数上限可配置化 → v0.1.0

## 分工(文件互斥,并行开发)

- 主控:lib.rs validate_uid、版本号、CI、cargo update、依赖清理、文档、集成测试、审查、提交
- 子代理 A(网络核心):session.rs / connection_manager.rs / relay.rs / app.rs / main.rs
- 子代理 B(Web 控制台):web.rs / static/index.html / protocol_filter.rs / parser.rs
- 子代理 C(存储与身份):storage.rs / identity.rs / ffi.rs / transport.rs

## 质量门禁(全部通过才可提交)

1. cargo fmt --check
2. cargo clippy --release --all-targets -- -D warnings
3. cargo test --release 全绿
4. cargo-audit -D warnings 零发现
5. 攻击性测试逐项验收(见上表)
