# Chrono-shift v0.0.8.3 安全说明

## 安全架构（与实现一致）

| 层 | 算法/机制 | 说明 |
|----|-----------|------|
| 会话层 | X25519 临时 DH + Ed25519 验签 + HKDF-SHA256 | 握手后所有帧 AES-256-GCM 加密；方向密钥防反射；单调序号防重放 |
| DC-Net 份额 | 好友边密钥 PSK (HKDF 派生) | 份额由线下建立的 pairwise 密钥派生，观察者无法重算 |
| 消息帧 | 长度前缀 + SHA-256 校验和 | 损坏/碰撞的 XOR 结果被检测丢弃，不产生垃圾文本 |
| 身份 | Ed25519 | 身份密钥 + 指纹；Web of Trust 验签入库 + 定点信任计算 |
| 中继 | Ed25519 签名 + 单调 nonce + 时间窗 + 限速 + TOFU 密钥固定 | 防重放/过期/放大/环回 |
| 存储 | WAL + 原子 checkpoint | 快照 tmp+fsync+rename；敏感文件 0600 |
| 随机数 | OsRng | 系统 CSPRNG |
| 内存 | zeroize | 密钥析构清零 |

## 威胁模型

### 对抗的攻击者

- **全流量被动观察者**：能看到并记录所有 TCP 流量；
- **主动中间人**：能注入/篡改/重放网络帧；
- **组内恶意成员**：正常参与 DC-Net 轮次但试图去匿名化/破坏；
- **恶意中继**：转发流量但试图放大/伪造。

### 提供的保证

- **传输保密性**：握手成功后所有帧 AES-256-GCM 加密（会话密钥来自临时 X25519 DH，
  非公钥可推）；
- **发送者匿名**（组内）：每轮每个成员都提交份额，份额对无密钥者为伪随机，
  无法确定消息嵌入者；
- **身份认证**：已知密钥的节点握手验签；中继消息 TOFU 固定密钥；
- **崩溃安全**：WAL + 原子 checkpoint。

### 不提供的保证（明确边界）

- **流量分析抵抗**：消息长度/时序未全局混淆（帧内填充为固定长度，但发送时机可见）；
- **全局匿名**：DC-Net 只覆盖频道组成员；跨组/中继场景不提供发送者匿名；
- **1:1 私聊 E2E**：ratchet 模块保持弃用（设计缺陷未接线），私聊请使用频道
  DC-Net 或外置工具；
- **抗量子**：Ed25519/X25519 均为经典密码学。

## 报告漏洞

见 [SECURITY_POLICY.md](SECURITY_POLICY.md)。

## 已知历史问题（已修复）

- v7.6 及之前：DC-Net 份额由公开信息派生（假 DH）→ 已在 P1/P2 修复；
- v8.1：会话密钥由公钥派生（假加密）+ 数据路径明文 JSON → 已在 v0.0.8.2 修复；
- 2026-08 代码审查：web 控制台 CORS 通配符、无界请求行/body → 已修复。
