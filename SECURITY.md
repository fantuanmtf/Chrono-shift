# Chrono-shift v7.0 安全说明

## 安全架构

纯 Rust 实现，零 unsafe 关键路径。内存安全由 Rust 编译器保证。

### 加密体系

| 层 | 算法 | 实现 |
|----|------|------|
| 传输加密 | AES-256-GCM | `aes-gcm` crate, 硬件 AES-NI 加速 |
| 密钥协商 | X25519 ECDH | Double Ratchet DH 棘轮 |
| 密钥派生 | HKDF-SHA256 | 根密钥 → 链密钥 → 消息密钥 |
| 前向安全 | Double Ratchet | 每条消息独立密钥, 旧密钥可删除 |
| 后向安全 | DH 棘轮 | 新密钥对替换, 未来消息安全 |
| 匿名 | DC-Net XOR | 信息论安全 (无条件匿名, 非计算安全) |
| 洗牌承诺 | SHA-256 + Merkle | 可验证, 防作弊 |
| 完整性 | SHA-256 链哈希 | 防篡改, 防重放 |
| 随机数 | OsRng | FIPS 140-2, 硬件熵源 |
| 恒定时间 | `constant_time_eq` | 防时序侧信道 |
| 内存清零 | `zeroize` crate | Drop 时自动安全擦除 |

### 匿名保证

DC-Net 提供**信息论安全**匿名，不同于 Tor/I2P 的计算安全:

- **Tor/I2P**: 基于计算困难性 (RSA/ECC), 可能被量子计算机攻破
- **DC-Net**: 基于信息论 (XOR 性质), 任何计算能力都无法攻破

即使攻击者:
- 监控全网所有通信 ✓
- 控制部分节点 ✓
- 拥有无限计算能力 ✓

仍然**无法确定消息发送者**。

## 威胁模型

### 已防护

- [x] 窃听 (AES-256-GCM 加密)
- [x] 流量分析 (DC-Net XOR 混淆)
- [x] 消息篡改 (SHA-256 链哈希)
- [x] 重放攻击 (msg_idx 单调递增)
- [x] 时序攻击 (恒定时间比较)
- [x] 内存泄漏 (zeroize 自动清零)
- [x] DNS 泄漏 (直连 IP, 无 DNS)
- [x] 缓冲区溢出 (Rust 边界检查)
- [x] 数据竞争 (Mutex 保护)
- [x] 字符串截断 (simdutf8 UTF-8 验证)
- [x] 掉线 (信誉系统自动检测 + 群组重组)
- [x] 作弊节点 (可验证洗牌 + Blame 追责)

### 待完善

- [ ] NAT 穿透 (需 WebSocket 信令)
- [ ] 中继节点 (当前仅直连, 需好友中转)
- [ ] x25519-dalek (当前简化 DH, 2 个测试失败)

## 漏洞报告

发现安全漏洞请发送至:
- Email: haiyanfurry@proton.me
- GitHub Issues: 标记 `security` 标签

维护者: haiyanfurry

## 审计历史

| 日期 | 版本 | 范围 | 结果 |
|------|------|------|------|
| 2026-05 | v4.0 | 全量 CVE (347,868) | 0 CRITICAL 适用 |
| 2026-05 | v4.0 | 代码审计 (C++ + Rust) | DNS 泄漏 + 弱随机数已修复 |
| 2026-06 | v7.0 | C++ 全量删除 | Rust 100%, 攻击面显著减小 |

GPLv3
